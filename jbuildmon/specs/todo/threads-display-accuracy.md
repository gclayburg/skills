  so we need to fix a few things and the output when we use the dash dash threads option sometimes actually a lot of times we don't see all of the agents that are running in parallel threads so for this build here you can see the output shows I got the overall in progress line at the bottom that that's working fine. What's not quite so Fein is the output when you do the dash dash threads were showing in this output. Here were showing only one stage being run at the time the one called policy start space bounce that one's running, but that is not actually correct. Well that that is correct is actually there should be at least one more stage of being run in parallel for the spill job that our bill get tool is not seeing. It's not reporting that it's being run even though I know it so we need to find out why that's the case and as a part of that, I'll I'll give you the command that I ran to see this that you can look at but what I want to do is the generate a an another integration test scenario that will show this kind of output so we've got one integration test scenario now where it runs a bill job and it monitors it I want another one where it can run the same style of build job this sort of patterned after the one I'm going to give you here, but it's something that's repeatable and bundled entirely within a repository and you can see exactly what Jenkins file pipeline is being used to gener all of the output for build get when we trigger that build because I think there's something weird going on with parallel stages and how they're nested inside of this pipeline job that's causing our bill get tool to be confused so I wanna write an integration test scenario around that situation so that the integration test phase on a normal bill can run this build job. We know the exact outputs what they should be doing and interpret the results so I'm gonna give you the exact command I ran and I want you to inspect this bill and I'm not I'm gonna give you what the definition of that build job is as well so you can see what it is, but I don't want that to be part of our normal integration test? I want you to create a new integration test this sort of patterned after that bill because there's lots of other things in there that really are the don't matter for the bill get to itself. I want to be able to create this unit test to test the output of the bill to get to itself and kind of simplify all of the other things as far as what the job is actually doing so what I'm thinking is you can figure something out too, but when I'm baking is part of these build part of this integration test build job needs to do something like do very minimal things like sleep a little bit longer than what is required for – – threads option. The pulling of long is like five seconds I might be wrong about that, so he made some task to do it like a sleep command longer than the pulling interval for the bill to get tool so we can we can do we can monitor and see the bill to look at when it's running. I can see that there is a job running and still running and I want to build this display that on the screen, I hope this makes sense in the end, we need a new integration test that runs slightly longer in the current one. I think it's called integration or build to get – integration – test so I need to create another one and Jenkins that's it's similar, but it's gonna be a little bit longer lasting and a little bit more nested kind of scenario parallel stage as well. I'll give you the exa inputs to use to make a pattern to decorate this test. OK any questions? Let me know.


buildgit was running with this command
```
buildgit --job panorama_integration_tests --threads build
```

but only the 'policyStart bounce'' stage was seen with the --threads option:
```

[10:19:31] ℹ   Stage: [agent6 guthrie] Declarative: Checkout SCM (<1s)
  [agent6 guthrie] policyStart bounce [========>           ] 48% 28s / ~57s
IN_PROGRESS Job panorama_integration_tests #1783 [====>               ] 29% 30s / ~1m 44s
```

The Jenkinsfile used for this build job is found in jbuildmon/testcasedata/Jenkinsfile-panorama-integration-tests



OK so again, so what we wanna do is a couple things too one is I wanted an integration test to be able to catch this issue like you talked about but I also want to make sure that we fix the code too so we need to create the inte prove that we run the bill to get tool that we do see the incorrect output and then we will do the integration will build the code change the code so this case doesn't happen then run the integration test again to verify that the problem is fixed so basically red green red, which means creative integration test that's going to fail watch it fail as we expect and then implement the code to fix the problem run it again and see that it turns green so first red then it's green