 we need to add a new option for Bill get so – – threads that option is present and we do like a real time build or a push or a status that chef any one of those real-time monitoring commands if the user also specifies a dash dash threads I want to expand our notification area at the bottom right now we show things like in progress and put a graph the status overall status of the bill for how far it's gone relative to what the estimated completion time is so what I wanna add. Here is extra lines that are the same the same kind of status thing at the bottom of the console window so that we will show what stage we are running at in the building so if there's a pipeline build and there's four stages to run each sequential. It should display the first stage as it runs and when that's done I'll display the next stage when that's done. We'll play the next stage, but all of that is shown in one line one line above the in progress line at the bottom so once again we have four sequential stages in the pipeline build stage one stage two stage three stage four it would start showing stage one with a graph and estimated completion time based on the last build time the last successful bill just like we do for the progress and then when that that stage has completed and next one starts it'll rewrite that line and just show the name of the second stage along with the completion graph that shows how far along we are in the that's individual stage and it'll show the name of the stage just like we do now for the stage output the line output for the build. You're the same name scheme we use for that but we're gonna show that at the bottom on the progress bar area. We also need to make sure we show the name of the agent running it so like agent six Guthrie or agent eight whatever the name is, we should already have that information s as part of the build anyway because we print that it's line when we do a bill push or status – you already got that information so I just wanna display at the bottom so basically the point of all this is the show the user running in a TTY what what individual stage of being running and how far along it is in that stage so this stage is a long stage running running 10 minutes or so we should be able to get an indication to the user that this stage has been running for a while. I was gonna run for a while and you can see that and when it's finished and erase that line and do the next stage this being run on that same agent also does what it does now which is so the regular line output of the running log line. I guess the right way to put it with with the timestamp and all that you can kind of see that how I do that now so which one augment this display so we can show the status of each individual stage that's running and how far along it is in progress of completing that stage and also this means if we do a parallel bill then we do the same thing we have if we have more than one agent running at the same time for this bill, which is gonna happen. I want to show multiple lines so if I've got three agents running and we're doing a parallel bill, but I wanna show each one of those agents on the line of its own, and what stage actually running along the progress does that make sense?