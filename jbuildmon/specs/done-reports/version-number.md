 we need some way to specify a version number or not specify, but we need to query the version number so Bill get – – version should show us what version we're currently are running and that's it like version version 1.0.0 or something which also means we need to find our figure out a way that we can start versioning this software so let's start with version number 1.0.0 and we're gonna use cement Samantha versioning we just need to come up the way come up with a way that we can keep the version number with the code itself and tag it to the to get repository so we can tag the version number when it's time to do a release and we can do things like put that version number in t and what else make sure the – – version option will show us the version of runtime somewhere else do all the things we need to do to keep track of version in the same way that makes sense and will bump the version number on our manual basis but for now start with version 1.0.0